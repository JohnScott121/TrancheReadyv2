# --- Required ---
PORT=10000

# Public URL of this app (used to build verify/download links)
APP_ORIGIN=http://localhost:10000

# Allowed marketing origin for CORS (your landing site)
MARKETING_ORIGIN=http://localhost:5500

# Short-lived verify link lifetime (minutes)
VERIFY_TTL_MIN=60

# Optional: Ed25519 signing (base64). Generate a keypair; store private on server.
# SIGN_PUBLIC_KEY will be shown on /verify to validate client-side.
SIGN_PRIVATE_KEY=
SIGN_PUBLIC_KEY=

# Optional: Stripe
STRIPE_SECRET_KEY=
STRIPE_PRICE_ID_TEAM=price_xxx_monthly
STRIPE_PRICE_ID_STARTER=price_yyy_oneoff
